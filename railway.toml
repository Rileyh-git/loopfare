[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start -w @loopfare/api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

[service]
internalPort = 4021
