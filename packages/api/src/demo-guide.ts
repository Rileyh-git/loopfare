export function demoGuide() {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Loopfare testnet demo</title><style>body{max-width:800px;margin:60px auto;padding:0 24px;background:#171420;color:#f5f3ef;font:18px/1.65 system-ui}a{color:#c8ff68}pre{padding:20px;background:#292235;overflow:auto;border-radius:12px}h1{font-size:42px;line-height:1.1}</style></head><body><a href="/">← Loopfare</a><h1>Your first paid API call.<br>Test funds only.</h1><p>The hosted beta uses Base Sepolia, not real-money USDC. Use a disposable test wallet. Never import or fund a production wallet for this demo.</p><h2>1. Install and check</h2><pre>npm install -g @loopfare/cli
loopfare --version
loopfare doctor</pre><p>Check the release notes for the currently published package. Some safety commands require the next release.</p><h2>2. Create a test wallet and budget</h2><pre>loopfare wallet create
loopfare budget set --daily 1</pre><p>Already have a local wallet? Do not replace it unless its key is securely backed up. Budget setup signs a domain-bound ownership message; your key stays local.</p><h2>3. Get Sepolia test funds</h2><p>Use the <a href="https://portal.cdp.coinbase.com/products/faucet">testnet faucet</a>. Select Base Sepolia and test USDC. Faucet availability and prerequisites may vary.</p><h2>4. Call the demo</h2><pre>loopfare call YOUR_LOOPFARE_ORIGIN/demo/v1/fortune --network base-sepolia --max-price 0.01</pre><p>Replace YOUR_LOOPFARE_ORIGIN with this website's origin. An initial HTTP 402 is a price challenge, not a failed user session. The client signs, retries, and reports settlement metadata. This fortune endpoint is a protocol demonstration, not commercial demand.</p><h2>Try a useful seller-owned endpoint</h2><p>The repository includes a bounded text-analysis origin example. It returns word/character counts and a content hash without storing text. Configure origin authentication before enabling it; see the <a href="/docs/seller-manual">seller manual</a>.</p><p>If a response is lost after signing, the budget reservation stays pending until reconciled. Do not repeatedly retry to bypass the cap.</p><p><a href="/docs/troubleshooting">Troubleshooting</a> · <a href="/docs/security-model">Security and telemetry</a></p></body></html>`;
}
